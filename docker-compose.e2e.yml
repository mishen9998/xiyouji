# Optional local/CI override for the distributed end-to-end test.
# The default compose file keeps app ports private; this file publishes them
# only while the test needs to connect to each instance directly.
services:
  app-1:
    ports:
      - "18081:8081"
  app-2:
    ports:
      - "18082:8082"
